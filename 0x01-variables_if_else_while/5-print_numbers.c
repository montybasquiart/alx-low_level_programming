#include <stdio.h>
#include <stdlib.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	/* Declare variable and initialize range */

	int n;

	for (n = 0; n < 10; n++)
	{

	/* print the value of the variable */

		printf("%d", n);
	}

	printf("\n");

	return (0);
}
