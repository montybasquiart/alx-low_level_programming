#include <stdlib.h>
#include <stdio.h>

/**
 * addition - adds two integers
 * @a: first paremeter
 * @b: second parameter
 * Return: adds final result
 */
int add(int a, int b)
{
  return (a + b);
}
