#include <stdlib.h>
#include <stdio.h>

/**
 * subtract - subtract two integers
 * @a: first paremeter
 * @b: second parameter
 * Return: subtract final result
 */
int sub(int a, int b)
{
  return (a - b);
}
