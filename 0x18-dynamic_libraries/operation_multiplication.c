#include <stdlib.h>
#include <stdio.h>

/**
 * multiplication - multiply two integers
 * @a: first paremeter
 * @b: second parameter
 * Return: multiplies final number
 */
int mul(int a, int b)
{
  return (a * b);
}
