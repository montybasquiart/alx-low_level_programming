#include <stdlib.h>
#include <stdio.h>

/**
 * modulus - mod of two integers
 * @a: first paremeter
 * @b: second parameter
 * Return: checks absolute value of final number
 */
int mod(int a, int b)
{
  return (a % b);
}
